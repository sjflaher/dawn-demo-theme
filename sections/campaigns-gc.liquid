{% schema %}
  {
    "name": "campaigns-liquid-gc",
    "settings": []
  }
{% endschema %}

  <div><h2>Some stuff here!</h2></div>

{% for campaign in shop.metaobjects["campaign"].values %}
  {{ campaign.featured_image }}
  <h1>{{ campaign.title }}</h1>
  <h2>{{ campaign.description }}</h2>
  <span>{{ campaign.start_date }}</span>
  <span>{{ campaign.end_date }}</span>
  {% for product in ampaign.campaign_products.values %}
    <span>{{ product.title }}</span>
  {% endfor %}
{% endfor %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
  console.log({{shop.metaobjects}})
{% endjavascript %}