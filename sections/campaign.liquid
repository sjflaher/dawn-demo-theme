{% schema %}
  {
    "name": "campaign-liquid",
    "settings": []
  }
{% endschema %}

{{ page.handle }}
{% assign campaign = shop.metaobjects.campaign[page.handle] %}
<h1>{{ campaign.title }}</h1>

{{  campaing | json }}

<div class="banner">BANNER</div>
<div class="products">
  {% for p in campaign.campaign_products %}
    <h2>{{ product.title }}</h2>
    <p>{{ product.description }}</p>
    <p>Price: {{ product.price | money }}</p>
    <img src="{{ product.featured_image.src }}" alt="{{ product.title}}">
  {% endfor %}
</div>



{% stylesheet %}
  .products {
    display: flex;
    flex-wrap: wrap;
  }

  
  .products a {
    margin: 20px;
    text-decoration: none;
  }
{% endstylesheet %}

{% javascript %}
{% endjavascript %}