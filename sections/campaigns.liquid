{% schema %}
  {
    "name": "campaigns-liquid",
    "settings": []
  }
{% endschema %}

{% for campaign in shop.metaobjects["campaign"].values %}
  <h1>{{ campaign.title }}</h1>
  <h2>{{ campaign.description }}</h2>
  <span>{{ campaign.start_date }}</span>
  <span>{{ campaign.end_date }}</span>
  <span>{{ campaign.campaign_products.length }}</span>
{% endfor %}


{% stylesheet %}
{% endstylesheet %}

{% javascript %}
  console.log({{ shop.metaobjects['campaign']['campaign-number-1'] | json }});
{% endjavascript %}